package it.eng.model;

import it.eng.model.SwaggerModel;

public class Path {
	public String pattern;
	public SwaggerModel.Path path;
	
	public Path(String pattern, MethodEntry methods[]) {
		this.pattern = pattern;
		this.path = new SwaggerModel.Path(methods);
	}
}