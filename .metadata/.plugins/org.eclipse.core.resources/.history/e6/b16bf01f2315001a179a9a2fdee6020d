package it.eng.model;

import it.eng.model.SwaggerModel.MethodEntry;
import it.eng.model.SwaggerModel.Path;

public class PathEntry {
	public String pattern;
	public Path path;
	
	public PathEntry(String pattern, MethodEntry methods[]) {
		this.pattern = pattern;
		this.path = new Path(methods);
	}
}